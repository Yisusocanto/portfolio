---
import { Icon } from "astro-icon/components";

interface Props {
	icon?: string;
}

const { icon } = Astro.props;
---

<span
	class="flex min-w-fit items-center gap-2 rounded-2xl p-2 bg-primary/10 hover:bg-primary/20 border"
>
	{icon && <Icon name={icon} class={"h-6 w-6"} />}
	<slot />
</span>
